name: 
on:
  merge_group:



jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Validate PR
      run: |
        echo "Failing tests as part of merge queue"
        exit 1