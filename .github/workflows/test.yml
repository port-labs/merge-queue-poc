name: 
on:
  merge_group:
    branches: 
      - main
    types: [checks_requested]



jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Validate PR
      run: |
        echo "Running tests as part of merge queue"